# Frozen from the fe-course conda env (python 3.11.15), where the full pipeline
# runs top to bottom with no errors.
#
#   conda create -n fe-course python=3.11 -y
#   conda activate fe-course
#   pip install -r requirements.txt
#
# Versions are pinned on purpose. pandas below 2.0 is compiled against numpy 1.x
# and will not import against the numpy 2.x pinned here: the dtype struct changed
# size, so any C extension built for 1.x fails at import with a binary
# incompatibility error. Pinning both together is what keeps that from happening.

# core numeric stack
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
pyarrow==25.0.1

# data acquisition
yfinance==1.7.0
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
python-dotenv==1.2.3

# modelling and statistics
scikit-learn==1.9.0
statsmodels==0.15.0
hmmlearn==0.3.3

# figures
matplotlib==3.11.1
seaborn==0.13.2

# notebook
jupyter==1.1.1
jupyterlab==4.6.3
ipykernel==7.3.0
nbconvert==7.17.1
nbformat==5.11.1

# serving a saved model
Flask==3.1.3
joblib==1.5.3
